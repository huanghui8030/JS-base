/**
 * 实现两个有序数组相加后，为一个有有序数据。
 * var a = [1,3,5,7,9],
 *     b = [2,6,8,10]
 */
function arrFn(aArr,bArr){
	var newArr = bArr;
	for (var i = 0 ; i < aArr.length ; i++) {
		for (var j = 0; j < bArr.length; j++) {
			
			if(arr[i] > brr[j]){

			}

		}
	}
	return newArr;
}